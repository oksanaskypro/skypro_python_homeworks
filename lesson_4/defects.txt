В классе StringUtils есть неточности:
В методе capitilize ошибка в написании слова (capitalize).
В методе  contains, система не корректно работает со строкой, которая не содержит, ни одного символа.

